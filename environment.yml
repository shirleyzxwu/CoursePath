name: coursepath
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - numpy
  - pip:
      - requests
      - sentence-transformers
      - playwright
      - torch               # required by sentence-transformers
