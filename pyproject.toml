[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "coursepath"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
markers = [
    "integration: live network tests, run with -m integration",
]
